# Database
DB_PATH=/data/energy_tracker.db
# Docker host path mounted to /data in the container (machine-specific)
BIOSIGNAL_DATA_DIR=./data
# Docker image tag to deploy (use immutable tag, e.g. commit SHA, in production)
BIOSIGNAL_IMAGE_TAG=latest

# Garmin credentials
GARMIN_EMAIL=your@email.com
GARMIN_PASSWORD=your_password
GARMIN_TOKEN_DIR=/data/.garmin_tokens

# HabitSync connection
HABITSYNC_URL=http://habitsync:6842
HABITSYNC_API_KEY=your_api_key

# Optional settings
TZ=Europe/London
SYNC_HOUR=6
SYNC_MINUTE_GARMIN=0
SYNC_MINUTE_ENVIRONMENT=5
ENVIRONMENT_LATITUDE=
ENVIRONMENT_LONGITUDE=
DEBUG=false
